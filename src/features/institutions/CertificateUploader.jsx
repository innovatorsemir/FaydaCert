import React from 'react';

const CertificateUploader = () => {
  return (
    <div>
      <h2>Certificate Uploader</h2>
      <p>Upload certificates with fields like name, degree, GPA, and issue date.</p>
    </div>
  );
};

export default CertificateUploader;
